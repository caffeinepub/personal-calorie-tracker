{
  "frontend/src/App.tsx": "Main app entry with TanStack Router setup, defining routes for dashboard, upload, and history pages, with a root redirect from / to /dashboard.",
  "frontend/src/components/Layout.tsx": "Persistent app shell with sticky header (logo + desktop nav), mobile bottom navigation bar, and footer with caffeine.ai attribution.",
  "frontend/src/components/StatCard.tsx": "Reusable stat display card showing a label, large numeric value with unit, optional icon, and color variants (success/warning/danger/info/default).",
  "frontend/src/components/FoodEntryCard.tsx": "Displays a single food log entry with thumbnail image, food name, date, calorie badge, and an optional delete button (Trash2 icon) that accepts `onDelete` and `isDeleting` props for inline loading state during deletion.",
  "frontend/src/components/ImageDropzone.tsx": "Drag-and-drop image upload zone with file input fallback, drag-over visual feedback, image preview with clear button, and image-only file validation.",
  "frontend/src/utils/calorieEstimator.ts": "Client-side heuristic calorie estimator that maps food keywords in filenames to calorie ranges and uses file size as a portion-size signal.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions including food entries CRUD (with new `useDeleteFoodEntry` mutation), daily summary, steps queries, `useLogSteps` mutation, `useCalorieLimit` query, `useSetCalorieLimit` mutation, and `useSevenDayHistory` query that fetches the last 7 calendar days of calorie data.",
  "frontend/src/pages/Dashboard.tsx": "Daily dashboard page with date navigation, stat cards, calorie limit card (with editable input, progress bar, and calories-remaining display), 7-day calorie history bar chart (Recharts) with reference line at the daily limit, exceeded-limit destructive alert banner, step logger, and food entry list with per-entry delete buttons wired to the `useDeleteFoodEntry` mutation.",
  "frontend/src/pages/FoodUpload.tsx": "Multi-step food logging page with image dropzone, automatic calorie estimation, editable food name/calorie fields, upload progress, and success confirmation. Now generates a unique `id` (timestamp + random string) via `generateId()` and passes it to `addFoodEntry.mutateAsync` to satisfy the updated mutation signature.",
  "frontend/src/pages/History.tsx": "History log page listing all tracked dates with per-day calorie summaries, average stats card, deficit/surplus badges, and click-to-navigate to that day's dashboard.",
  "frontend/src/index.css": "Global styles with OKLCH health-wellness color system (forest green primary, warm orange accent, success/warning tokens), Inter + Plus Jakarta Sans fonts, and utility classes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families, success/warning color tokens, enhanced border-radius scale, and card shadow utilities."
}